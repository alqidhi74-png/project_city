import { OverviewContent } from '@/components/dashboard/OverviewContent';

/** Overview reads everything it needs from the layout's context providers. */
export default function OverviewPage() {
  return <OverviewContent />;
}
